# On Windows, override target-dir to avoid OneDrive sync issues:
#   Set CARGO_TARGET_DIR=C:\Users\tenis\.forge-build\target
#   (or uncomment the line below on Windows only)
#
# On macOS, the default target/ directory works fine.
#
# [build]
# target-dir = "C:\\Users\\tenis\\.forge-build\\target"
